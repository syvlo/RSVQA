# Ce fichier de configuration est utilise pour définir les emplacements de la base de données

# Fichiers contenant les images, questions, réponses et personne (ce dernier étant peu utilisé pour le moment)
images_file = "USGSimages.json"
questions_file = "USGSquestions.json"
answers_file = "USGSanswers.json"
people_file = "USGSpeople.json"
# Chemin de sauvegarde des images de la base de données
image_folder = "dataUSGS/"

# Si besoin, dans le futur, pour signer la base
secret_key = "This should not be exposed as this is the secret key (and something to make it more special @WUR) :)"
